[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm', 'openssl']
aptPkgs = ['ca-certificates']

[phases.install]
cmds = ['npm install --legacy-peer-deps']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm run start'

[variables]
NODE_ENV = 'production'
NODE_VERSION = '18' 